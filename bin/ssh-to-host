#!/usr/bin/env opsh
# shellcheck shell=bash

source "$SCRIPTDIR/../lib/pulumi.opsh"

pulumi::node::check-index "$@"

pulumi::node::ssh::flags SSH_FLAGS "$1"
shift

# shellcheck disable=SC2029
ssh "${SSH_FLAGS[@]}" "$@"

# vim:set ft=sh:
